# Fixture Fitness

This fixture represents the cleanest bootstrap topology:

- both `AGENTS.md` and `CLAUDE.md` already exist
- the repo exposes root-safe commands through `make`
- default local `entrix run` should stay green with no CI-only exceptions
